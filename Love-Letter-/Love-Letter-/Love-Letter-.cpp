
#include <iostream>
#include "Card.h"
#include "Player.h"
#include "Game.h"

int main()
{//Card* card = new Baron();
	//Card* card2 = new Countess();
	//Card *card = new Card();	
	//Card *card2 = new Card();
	//Player *player = new Player("Gicu",card,card2,0);
	//std::vector<Card> cards = { Guard(), Guard(), Guard(), Guard(), Guard(), Priest(), Priest(), Baron(), Baron(), Handmaid(), Handmaid(), Prince(), Prince(), King(), Countess(), Princess() };
	
	//Game game;
	//game.playerDrawCard(player, cards);
	//game.playerDrawCard(player, cards);
	//std::cout << player->getFirstCard()->getName() << std::endl << std::endl;
	//std::cout << player->getSecondCard()->getName() << std::endl << std::endl;

	//player->drawCard(player->getFirstCard(), nrOfCards, cards);
	//std::cout << player->getFirstCard().getNumber();
	//player->drawCard(player->getFirstCard(), cards);
	//std::cout << player->getFirstCard()->getName()<<std::endl << std::endl;
	//player->drawCard(player->getFirstCard(), cards);
	//std::cout << player->getFirstCard()->getName() << std::endl << std::endl;
	//player->drawCard(player->getFirstCard(), cards);
	//std::cout << player->getFirstCard()->getName() << std::endl << std::endl;

	Game game;
	game.SetStartingPlayers();
	//game.test();
	
}

